<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MainActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.LauncherActivity" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="Theme.CameraActivity" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowAnimationStyle">@style/camera_activity_anim</item>
    </style>

    <style name="Theme.PictureViewActivity" parent="Theme.AppCompat.NoActionBar">
        <!--item name="android:windowFullscreen">true</item-->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowBackground">@color/darkgrey</item>
    </style>

    <style name="Theme.FlatViewActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <!--将状态栏的字体黑色（适应高亮的状态栏背景色）-->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowBackground">@color/tranDarkGrey</item>
    </style>

    <style name="Theme.ImgSelectActivity" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="Theme.ImgProcessActivity" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/darkgrey</item>
    </style>
    <style name="Theme.MoveSelectActivity" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

</resources>